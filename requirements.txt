# Python side of the build: the kernel that executes the corpus's code cells
# under `myst build --execute`, plus the server mystmd drives it through.
jupyter-server
ipykernel
numpy
scipy
matplotlib
pandas
plotly
quantecon
